get:
  tags:
    - Cancellation
  summary: Cancellation closing process
  description: Cancellation closing process
  operationId: cancellationClosingProcess
  parameters:
    - name: id
      in: path
      description: Opportunity Id
      required: true
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: Success
      content:
        application/json:
          schema:
            $ref: ..\components\schemas\policyCancellationClosingProcess_Type.yaml
    '400':
      description: Bad Request.
      content:
        application/json:
          schema:
            $ref: ..\components\schemas\apiErrorResponse_Type.yaml
    '500':
      description: Server Error.
      content:
        application/json:
          schema:
            $ref: ..\components\schemas\apiErrorResponse_Type.yaml
