openapi: 3.1.0
info:
  version: 1.4.0.20230227
  title: ACORD Messaging Library Common Objects
  description: Common objects for current AML release.
  termsOfService: https://www.acord.org/terms-of-use
  contact:
    name: ACORD
    url: http://www.acord.org
    email: memberservices@acord.org
  license:
    name: ACORD Terms of Use
    url: https://www.acord.org/terms-of-use
servers:
  - url: >-
      https://virtserver.swaggerhub.com/SteadfastTech/InsurerAPI-ST-TEST/1.4.0.20230303
    description: SwaggerHub API Auto Mocking
paths:
  /renewalQuotationProcess/{id}:
    $ref: paths/renewalQuotationProcess_{id}.yaml
  /renewalQuotationProcessResult/{id}:
    $ref: paths/renewalQuotationProcessResult_{id}.yaml
  /renewalOrderProcess/{id}:
    $ref: paths/renewalOrderProcess_{id}.yaml
  /renewalOrderProcessResult/{id}:
    $ref: paths/renewalOrderProcessResult_{id}.yaml
  /renewalClosingProcess/{id}:
    $ref: paths/renewalClosingProcess_{id}.yaml
  /renewalClosingProcessResult/{id}:
    $ref: paths/renewalClosingProcessResult_{id}.yaml
components:
  securitySchemes:
    sctpUsername:
      type: apiKey
      in: header
      name: SCTP-USERNAME
    sctpPassword:
      type: apiKey
      in: header
      name: SCTP-PASSWORD
security:
  - sctpUsername: []
    sctpPassword: []
